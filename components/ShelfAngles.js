import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ShelfAngles = () => {
  return (
    <View>
      <Text>ShelfAngles</Text>
    </View>
  );
};

export default ShelfAngles;

const styles = StyleSheet.create({});
