import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PlasterBoard = () => {
  return (
    <View>
      <Text>PlasterBoard</Text>
    </View>
  );
};

export default PlasterBoard;

const styles = StyleSheet.create({});
