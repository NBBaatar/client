import { StyleSheet, Text, View } from "react-native";
import React from "react";

const FireCable = () => {
  return (
    <View>
      <Text>FireCable</Text>
    </View>
  );
};

export default FireCable;

const styles = StyleSheet.create({});
